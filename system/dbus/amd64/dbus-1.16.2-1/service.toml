schema_version = 1

[service]
name = "dbus"
description = "D-Bus System Message Bus"
command = ["/usr/bin/dbus-daemon", "--system", "--nofork", "--nopidfile"]
user = "messagebus"
group = "messagebus"
working_dir = "/"
restart = "always"
type = "simple"
after = ["local-fs"]
before = ["network"]
