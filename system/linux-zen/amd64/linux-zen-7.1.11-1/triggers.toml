schema_version = 1
name = "linux-zen-initramfs"
description = "Regenerate initramfs images after Linux-Zen module changes"
on_paths = ["usr/lib/modules/**"]
exec = ["/usr/bin/initramfs-generator", "-P"]
priority = 30
events = ["post-change", "post-remove"]
ignore_missing_binary = false
